({
    
	handleClick: function(cmp, event, helper) {
        helper.handleClick(cmp, event);
    },
    
    handleCaptureEvent: function(cmp, event, helper) {
        helper.handleCaptureEvent(cmp, event);
    },
})